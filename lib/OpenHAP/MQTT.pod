=head1 NAME

OpenHAP::MQTT - MQTT client wrapper for OpenHAP

=head1 SYNOPSIS

    use OpenHAP::MQTT;

    my $mqtt = OpenHAP::MQTT->new(
        host => '127.0.0.1',
        port => 1883,
    );

    $mqtt->mqtt_connect();
    $mqtt->subscribe('stat/device/POWER', sub { ... });
    $mqtt->publish('cmnd/device/POWER', 'ON');

=head1 DESCRIPTION

This module is a wrapper around L<Net::MQTT::Simple>. It loads
L<Net::MQTT::Simple> with C<require>, thus the dependency stays
optional. Without the dependency, the daemon starts and serves HomeKit,
but no device state moves. The connections are plaintext. There is no
TLS.

=head1 SEE ALSO

F<spec/MQTT.md>, F<spec/MQTT-Transport.md>

=cut
